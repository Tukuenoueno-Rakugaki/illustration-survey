services:
  - type: web
    name: illustration-survey
    env: python
    buildCommand: ""
    startCommand: python3 server.py
    envVars:
      - key: SURVEY_DATA_DIR
        value: /var/data
    disk:
      name: survey-data
      mountPath: /var/data
      sizeGB: 1
